import 'package:flutter/material.dart';

class ResultSuccessIcon extends StatelessWidget {
  const ResultSuccessIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
